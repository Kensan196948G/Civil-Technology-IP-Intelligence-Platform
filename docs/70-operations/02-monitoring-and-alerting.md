# 📊 監視・アラート

## 0. 現行の実装（2026-09-10〜）

Deep Debug 2026-09-10 で「**設計書は外部合成監視を宣言しているが実行機構が無い**」ことが
判明したため、ホスト上の systemd user unit として実装した。

| 項目 | 値 |
|---|---|
| 実行 | `ctiip-healthcheck.service`（oneshot） |
| スケジュール | `ctiip-healthcheck.timer`（**5分ごと**、`OnBootSec=2min` / `OnUnitActiveSec=5min`） |
| 実装 | `scripts/healthcheck.sh` |
| 手動実行 | `systemctl --user start ctiip-healthcheck.service` |
| 結果 | `journalctl --user -t ctiip-healthcheck` ／ `~/.local/state/ctiip-healthcheck/last-run.log` |
| 失敗の確認 | `systemctl --user --failed`（終了コード 1 で unit が failed になる） |

### 検知する内容

| # | 項目 | 判定 |
|---|---|---|
| 1 | 公開URLの疎通 | `https://ctip…/api/health` と `https://ctiip-mvp…/api/health` が **HTTP 200** を返すこと |
| 2 | DB 到達性 | `/api/health` の `db` が `ok` であること（`error` なら 503 + `status:"degraded"`） |
| 3 | **版数ドリフト** | `/api/health` の `version`（ビルド時に埋め込んだ commit）が `origin/main` と一致すること |
| 4 | MVP ローカル | `127.0.0.1:3001` が応答しない場合、`ctiip-mvp-web.service`（user unit）を再起動 |

> **本番（system unit）の再起動は監視では行わない。** 本スクリプトの目的は「検知」であり、
> 復旧操作は承認された手順（[デプロイ手順](01-deployment-procedure.md)）で人間が実施する。

### この監視で検知できるようになった事象

いずれも 2026-09-10 の Deep Debug まで**無検知で継続していた**ものである。

| 事象 | 継続期間 | 検知する項目 |
|---|---|---|
| 本番が 47 コミット古いビルドで稼働 | 2026-08-29〜09-10 | #3（`version` 未報告＝旧実装） |
| MVP が HTTP 530 / Cloudflare error 1033 で恒久停止 | 〜09-10 | #1 |
| `/api/health` の `time` がビルド時刻で凍結 | 2026-09-01〜 | #3・#2（旧実装は `db` を返さない） |

### 未実装（残存リスク）

- メール・チャット等への**能動的通知は未実装**（journal と unit の failed 状態のみ）。
- エラー率（5xx）・応答時間 p95・ジョブ滞留・遅いクエリは未取得（§1.1／§1.2 の残項目）。
- 外形監視は**本ホスト内からの実行**であり、ホスト自体の停止は検知できない
  （本来は外部の監視サービスから実行する。⚠️ 要決定）。

## 1. 監視項目

### 1.1 可用性・性能

| 指標 | 取得元 | 閾値（⚠️ 要決定） |
|---|---|---|
| ヘルスチェック応答 | 外部合成監視 | 3回連続失敗 |
| エラー率（5xx） | Workers Analytics | 平常比の大幅増 |
| 応答時間 p95 | Workers Analytics | NFR-P-001/002 の目標超過 |
| 検索応答時間 | アプリ計測 | 同上 |
| DB 接続エラー | アプリログ | 発生 |
| DB 遅いクエリ | Neon | 一定時間超過 |

### 1.2 ジョブ

| 指標 | 閾値 |
|---|---|
| キュー滞留数 | ⚠️ 要決定 |
| DLQ 件数 | **1件以上で通知** |
| ジョブ失敗率 | ⚠️ 要決定 |
| バッチ実行時間 | 業務時間（8:00）に食い込む見込み |
| 取り込み件数の急変 | 前日比の大幅変動（取得元の仕様変更を疑う） |
| エージェント実行の滞留 | 一定時間を超えて running のもの |

### 1.3 セキュリティ

| 指標 | 閾値 |
|---|---|
| 認証失敗の連続 | ⚠️ 要決定 |
| 権限拒否（403/404）の急増 | 平常比の大幅増 |
| 大量エクスポート・ダウンロード | 件数上限に接近 |
| 管理者操作（権限変更） | **すべて通知** |
| C4 へのアクセス | **すべて通知** |
| 秘密スキャンの検出 | 発生時に即時 |

### 1.4 コスト

| 指標 | 頻度 |
|---|---|
| AI トークン消費（日次・月次） | 日次確認、予算比で警告 |
| Neon 容量・計算時間 | 週次 |
| R2 容量 | 週次 |
| Workers リクエスト数 | 週次 |

### 1.5 データ品質

| 指標 | 頻度 | 閾値 |
|---|---|---|
| Provenance 充足率 | 日次 | **100% 未満で通知** |
| 欠損率・重複率 | 週次 | ⚠️ 要決定 |
| 分類付与率 | 週次 | ⚠️ 要決定 |
| 名寄せ未確定件数 | 週次 | 増加傾向で通知 |
| `dq_issues` の未対応件数 | 週次 | 増加傾向で通知 |

## 2. アラート

| 重大度 | 例 | 通知先 | 対応時間 |
|---|---|---|---|
| **P1 緊急** | 認証が機能しない、機密の露出、全面停止、データ破損 | 即時（担当者 + システムオーナー） | 直ちに |
| **P2 重大** | 主要機能の停止、エラー率の急増、DLQ 滞留 | 即時（担当者） | 当日中 |
| **P3 注意** | 性能劣化、バッチ遅延、コスト超過の兆候 | 日次まとめ | 翌営業日 |
| **P4 情報** | データ品質の傾向、利用状況 | 週次まとめ | 定例で確認 |

⚠️ **要決定** — 通知先（メール・チャット）、当番体制、営業時間外の扱い。

## 3. ダッシュボード（運用向け）

| 区分 | 表示 |
|---|---|
| 可用性 | 稼働状況、エラー率、応答時間の推移 |
| ジョブ | キュー滞留、DLQ、実行中ジョブ、失敗率 |
| データ | 取り込み件数、Provenance 充足率、品質指標 |
| セキュリティ | 認証失敗、権限拒否、エクスポート件数 |
| コスト | AI トークン、DB、ストレージ |
| 利用 | 部署別アクティブ利用者数、検索件数、発明届件数 |

**利用率を運用指標に含める理由**: 本システムは「使われないこと」が最大の失敗要因である。
可用性が満点でも利用率が低ければ、導入は成功していない。

## 4. ログ

| 種別 | 出力先 | 保持 | 注意 |
|---|---|---|---|
| アプリログ | Workers Logs | 90日 | 機密本文・シークレットを出さない |
| 監査ログ | `audit_logs`（DB） | ⚠️ 要決定 | 改変不可 |
| ジョブログ | DB（`ai_runs` 等） | 恒久 | 入出力の要約のみ |
| アクセスログ | Cloudflare | 既定 | — |

**相関ID**: 1リクエストにつき1つ発行し、API・ジョブ・AI実行に伝播させる。
障害調査は相関IDを軸に行う。

## 5. 定期確認

| 頻度 | 内容 |
|---|---|
| 日次 | エラー率、DLQ、Provenance 充足率、AI コスト |
| 週次 | 性能推移、データ品質、preview 環境の残存、利用率 |
| 月次 | コスト集計、容量、アラートの妥当性見直し |
| 半期 | 権限棚卸し、復旧訓練、監視項目の見直し |
